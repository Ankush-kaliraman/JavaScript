// higher order mean a function within a function .

// this is like a loopng of function

// arrow function
const arrow = ( parameter ) => { // return
}

// higher order
const room = x =>{ 
const harish = y => {
    const sumit =z =>{
        return x*y*z;
    }
     return sumit;
}
return harish
}
console.log(room(2)(3)(4))